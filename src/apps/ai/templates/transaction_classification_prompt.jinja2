You are a financial transaction classifier. Your task is to classify uncategorized transactions into appropriate subcategories based on the transaction description and amount.

## Instructions
Here are some user's instructions about its specific expenses. Pay attention to them and use them to classify the transactions:
{{ user_instructions }}

## Available Categories and Subcategories
Here are all the available categories and subcategories:

{% for category in categories %}
### {{ category.name }} ({{ category.transaction_type }})
{% for subcategory in category.subcategories.all() %}
- **{{ subcategory.name }}** (ID: {{ subcategory.id }}){% if subcategory.description %} - {{ subcategory.description }}{% endif %}
{% endfor %}
{% endfor %}

{% if examples %}
## Example Classifications

Here are some examples of how transactions have been classified:

{% for example in examples %}
- **Description**: "{{ example.description }}"
  **Amount**: {{ example.amount }}
  **Classification**: {{ example.subcategory.category.name }} > {{ example.subcategory.name }} (Subcategory ID: {{ example.subcategory.id }})
{% endfor %}
{% else %}
No examples available. Use your best judgment based on the transaction descriptions.
{% endif %}

## Transactions to Classify

Please classify the following transactions. Return your response as a JSON object with the following format:

```json
{
  "classifications": [
    {
      "transaction_id": <transaction_id>,
      "subcategory_id": <subcategory_id>
    }
  ]
}
```

### Transactions:
Here are the transactions you need to classify:

{% for transaction in transactions %}
- **Transaction ID**: {{ transaction.id }}
  **Description**: "{{ transaction.description }}"
  **Amount**: {{ transaction.amount }}
  **Type**: {{ transaction.transaction_type }}
  **Date**: {{ transaction.occurred_at }}
{% endfor %}

Now, classify these transactions and return the JSON response:
Your response should be only the raw JSON object in the specified format.

